import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Sri Sandhoshi Madha Group of Institutions – Charitable Trust",
  description: "Transforming lives with love & compassion. Providing food, shelter, education, medical care, and empowerment for the differently-abled.",
  keywords: "charity, trust, differently-abled, education, medical care, empowerment, India",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
